CREATE TABLE brack_data (
  `gender` VARCHAR(255),
  `cyberbully_presence` VARCHAR(255),
  `age_group` VARCHAR(255),
  `religiosity_importance` INTEGER,
  `religiosity_attendance` INTEGER,
  `religiosity_strength` INTEGER,
  `age` INTEGER,
  `total_cyber_victim` INTEGER,
  `total_impulsivity` VARCHAR(255),
  `total_religiosity` INTEGER,
  `private_religiosity` INTEGER,
  `public_religiosity` INTEGER,
  `religious` INTEGER,
  `cb1_stealing_personal_info` INTEGER,
  `cb2_stealing_screen_names` INTEGER,
  `cb3_threatening_online` INTEGER,
  `cb4_insulting_online` INTEGER,
  `cb5_excluding_by_blocking` INTEGER,
  `cb6_posting_fake_photos` INTEGER,
  `cb7_sharing_private_conversations` INTEGER,
  `cb8_making_fun_online` INTEGER,
  `cb9_threatening_email` INTEGER,
  `cb10_stealing_passwords` INTEGER,
  `cb11_reading_personal_messages` INTEGER,
  `cb12_threatening_texts` INTEGER,
  `cb13_pretending_other_gender` INTEGER,
  `cb14_posting_embarrassing_photos` INTEGER,
  `cbv1_stealing_personal_info` INTEGER,
  `cbv2_stealing_screen_names` INTEGER,
  `cbv3_threatening_online` INTEGER,
  `cbv4_insulting_online` INTEGER,
  `cbv5_excluding_by_blocking` INTEGER,
  `cbv6_posting_fake_photos` INTEGER,
  `cbv7_sharing_private_conversations` INTEGER,
  `cbv8_making_fun_online` INTEGER,
  `cbv9_threatening_email` INTEGER,
  `cbv10_stealing_passwords` INTEGER,
  `cbv11_reading_personal_messages` INTEGER,
  `cbv12_threatening_texts` INTEGER,
  `cbv13_pretending_other_gender` INTEGER,
  `cbv14_posting_embarrassing_photos` INTEGER
);


CREATE TABLE survey_data (
  `pcode` VARCHAR(255),
  `age` INTEGER,
  `gender` VARCHAR(255),
  `ses` VARCHAR(255),
  `gradelevel` INTEGER,
  `ov1` INTEGER,
  `ov2` FLOAT,
  `ov3` INTEGER,
  `ov4` FLOAT,
  `ov5` FLOAT,
  `ov6` INTEGER,
  `ov7` FLOAT,
  `ov8` FLOAT,
  `ov` FLOAT
);

CREATE TABLE trolling_data (
  `recruitment_phase` INTEGER,
  `age` INTEGER,
  `gender` VARCHAR(255),
  `socialmedia_days_week` INTEGER,
  `socialmedia_hours_day` INTEGER,
  `internet_day` INTEGER,
  `uses_facebook` INTEGER,
  `uses_instagram` INTEGER,
  `uses_wordpress` INTEGER,
  `uses_snapchat` INTEGER,
  `uses_tumblr` INTEGER,
  `uses_whatsapp` INTEGER,
  `uses_twitter` INTEGER,
  `uses_wechat` INTEGER,
  `uses_pinterest` INTEGER,
  `uses_myspace` INTEGER,
  `uses_youtube` INTEGER,
  `uses_tiktok` INTEGER,
  `uses_other` VARCHAR(255),
  `social_media_platforms_total` INTEGER,
  `troll_victim` INTEGER,
  `troll_victim_count` INTEGER,
  `troll_perp` INTEGER,
  `troll_perp_count` INTEGER,
  `gait_perpetration_total` INTEGER,
  `cognitive_empathy_total` INTEGER,
  `affective_empathy_total` INTEGER,
  `psychopathy_total` INTEGER,
  `neg_social_potency_total` INTEGER,
  `sadism_total` INTEGER,
  `self_esteem_total` INTEGER,
  `platform_count` INTEGER
);
